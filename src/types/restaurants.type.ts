export interface Restaurant {
    id: number;
    name: string;
    address: string;
    image: string;
}

export interface Dish {
    id: number;
    name: string;
    description: string;
    image: string;
}